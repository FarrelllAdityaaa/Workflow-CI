name: Titanic-CI-Retrain

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: {type: int, default: 100}
      data_path: {type: str, default: "titanic_preprocessing.csv"}
    command: "python modelling.py {n_estimators} {data_path}"